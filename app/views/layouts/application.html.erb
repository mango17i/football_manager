<!DOCTYPE html>
<html>
  <head>
    <title>Pokétunes</title>
    <link href="https://upload.wikimedia.org/wikipedia/fr/thumb/6/6d/Looney_Tunes_Logo.png/330px-Looney_Tunes_Logo.png" rel="shortcut icon" type="text/css">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= yield %>
  </body>
  <script>
        let card = document.querySelector(".card-category");
        let cardToggle = document.querySelector(".toggle");
        let toggleCard = document.querySelectorAll(".card-category");
        let toggleNav = document.querySelectorAll(".toggle");

      let toggleNavLength = toggleNav.length;
      let showCard = function show(card){
        console.log('hihi')
      };
          toggleNav.forEach((toggle) => {
            toggle.addEventListener("click", (event) => {
              console.log("hello");
              toggleCard.forEach((element) => {
                console.log("world");
                element.classList.toggle("actiive");
              });
            });
        });
  </script>
</html>
